# 🌍 Travel Agency Management System

Информационная система для автоматизации работы туристического агентства. Кроссплатформенное десктопное приложение, позволяющее управлять турами, бронированиями, клиентской базой и финансовой отчетностью.

## 🚀 Функциональность

Система поддерживает ролевую модель доступа (RBAC) для следующих типов пользователей:

### 👤 Менеджер
*   Создание, редактирование и архивация туров.
*   Ведение базы клиентов, поиск по ФИО и телефону.
*   Оформление заявок, генерация договоров, смена статусов.

### 💰 Финансовый специалист
*   Учет входящих платежей .
*   Отслеживание неоплаченных или частично оплаченных броней.
*   Генерация финансовых отчетов и выгрузка в Excel.

### 🛡️ Администратор
*   Регистрация сотрудников, назначение ролей и должностей.
*   Просмотр журнала аудита.
*   Создание резервных копий базы данных.

### 🏖️ Клиент (Турист)
*   Просмотр доступных туров.
*   История поездок и текущий статус бронирования.
*   Возможность оставить заявку на бронирование.

---

## 🛠 Технологический стек

**Client Side (Desktop):**
*   **Язык:** Kotlin
*   **UI Framework:** Jetpack Compose Multiplatform (Desktop)
*   **Архитектура:** MVVM (Model-View-ViewModel)
*   **Навигация:** Voyager
*   **DI (Внедрение зависимостей):** Koin

**Data & Backend Logic:**
*   **СУБД:** PostgreSQL 16+
*   **ORM:** Exposed (Kotlin SQL Framework)
*   **Бизнес-логика:** Гибридная (Kotlin + хранимые процедуры PL/pgSQL)
*   **Экспорт данных:** Apache POI (генерация .xlsx)

---

## ⚙️ Установка и запуск

### 1. Предварительные требования
*   JDK 17 или выше.
*   PostgreSQL 14 или выше.
*   IntelliJ IDEA.

### 2. Настройка базы данных
1.  Создайте пустую базу данных с именем `TravelAgency` в pgAdmin.
2.  Найдите файл дампа `database_dump.sql` (или `init_db.sql`) в папке `database/` этого репозитория.
3.  Восстановите базу данных из дампа:
    *   **Через pgAdmin:** ПКМ по базе -> Restore -> Выбрать файл.
    *   **Через консоль:**
        ```bash
        psql -U postgres -d TravelAgency -f database_dump.sql
        ```
4.  **Важно!** Создайте технического пользователя для подключения приложения:
    ```sql
    CREATE USER client_app_bot WITH PASSWORD '87654321';
    GRANT ALL PRIVILEGES ON ALL TABLES IN SCHEMA public TO client_app_bot;
    GRANT ALL PRIVILEGES ON ALL SEQUENCES IN SCHEMA public TO client_app_bot;
    ```

### 3. Запуск приложения
1.  Откройте проект в IntelliJ IDEA.
2.  Дождитесь индексации и синхронизации Gradle.
3.  Запустите файл `src/main/kotlin/Main.kt`.

---

## 🔑 Тестовые учетные записи

Для проверки функционала используйте следующие логины и пароли:

| Роль | Логин (User DB) | Пароль |
| :--- | :--- | :--- |
| **Администратор** | `admin_ivanov` | `12345678` |
| **Менеджер** | `manager_petrov` | `12345678` |
| **Финансовый спец.** | `finance_smirnova`| `12345678` |

> **Примечание:** Пароли для сотрудников задаются на уровне ролей СУБД PostgreSQL. Если пароли не подходят, сбросьте их через SQL-запрос `ALTER USER username WITH PASSWORD 'new_password';`.

---

## 📂 Структура проекта

*   `data/` — Слои данных (модели, репозитории, настройки БД).
*   `ui/` — Пользовательский интерфейс (Экраны, Диалоги, Тема).
    *   `screens/` — Основные экраны разделенные по ролям (admin, auth, bookings, etc.).
*   `di/` — Модули Koin для внедрения зависимостей.
*   `util/` — Вспомогательные сервисы (Excel экспорт, Бэкап).
